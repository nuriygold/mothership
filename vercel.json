{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "prisma generate && ( [ \"$RUN_PRISMA_MIGRATE_DEPLOY\" = \"1\" ] && prisma migrate deploy || echo \"Skipping prisma migrate deploy during build (set RUN_PRISMA_MIGRATE_DEPLOY=1 to enable).\" ) && npm run build",
  "installCommand": "npm install --ignore-scripts",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/dispatch/worker",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/v2/email/triage/worker",
      "schedule": "*/30 * * * *"
    }
  ]
}
