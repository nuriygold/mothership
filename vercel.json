{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "DATABASE_URL=$DATABASE_POOLER_URL prisma migrate deploy --skip-advisory-locks && prisma generate && npm run build",
  "installCommand": "npm install --ignore-scripts",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/dispatch/worker",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/v2/email/triage/worker",
      "schedule": "*/30 * * * *"
    }
  ]
}
