# Required — set this to a long random string; paste it into the Claude console's Token field
TERMINAL_SECRET=change-me-to-something-secret

# Anthropic API key — used by the claude CLI inside the container
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PORT=3001
TERMINAL_COMMAND=claude
SESSION_TTL_MS=3600000
